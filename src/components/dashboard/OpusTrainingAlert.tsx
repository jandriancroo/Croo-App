// This component has been replaced by:
// - OpusBackgroundSync (background heartbeat for Theo's brain)
// - Temporary Tasks (OPUS training tasks show as regular task cards with OPUS logo)
// Keeping file to avoid import errors in case it's referenced elsewhere.
export function OpusTrainingAlert() {
  return null;
}
